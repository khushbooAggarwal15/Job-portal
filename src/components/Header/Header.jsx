import React from "react";
import { NavBar } from "../../styles/components/NavBar";
const Header = () => {
  return (
    <NavBar>
      <h1> Job-Portal</h1>
    </NavBar>
  );
};

export default Header;
